#include <Arduino.h>

// Keep the Arduino IDE sketch as the single firmware source of truth while
// giving PlatformIO its conventional src/main.cpp entry point.
#include "../fieldline_esp32_node.ino"
