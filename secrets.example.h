#pragma once

// Copy this file to secrets.h and enter the Wi-Fi used by the ESP32 and
// dashboard device. secrets.h is git-ignored so credentials are not committed.
#define FIELDLINE_WIFI_SSID "YOUR_WIFI_NAME"
#define FIELDLINE_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
